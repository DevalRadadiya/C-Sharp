/* 3. Write a C# Sharp program to print the result of dividing two numbers. */

using System;

class program3
{
    static void Main()
    {
        double a = 10, b = 2, c;
        c = a / b;
        Console.WriteLine(c);
    }
}